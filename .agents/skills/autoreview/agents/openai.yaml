interface:
  display_name: "Auto Review"
  short_description: "Codex-only structured closeout review"
  default_prompt: "Use $autoreview as a final Codex-only structured code review. Verify every finding against the real code before changing anything, rerun focused tests after fixes, and rerun autoreview until clean or until a remaining finding is explicitly rejected with rationale."
